# Proxmox VE MCP Server Configuration
# Copy this file to .env and fill in your values

# URL of your Proxmox VE instance (required)
# Include the port if non-standard (default PVE port is 8006)
PVE_BASE_URL=https://pve.example.com:8006

# API token ID (required)
# Format: user@realm!tokenname (e.g. root@pam!mcp)
# Create at: PVE UI > Datacenter > Permissions > API Tokens
PVE_TOKEN_ID=root@pam!mcp

# API token secret UUID (required)
# Shown only once when creating the token
PVE_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Access tier: "read-only", "read-execute", or "full" (default: full)
# PVE_ACCESS_TIER=full

# Comma-separated category allowlist (default: all categories)
# PVE_CATEGORIES=nodes,qemu,lxc,storage,cluster,tasks

# Disable TLS certificate verification for self-signed certs (default: true)
# PVE_VERIFY_SSL=false

# Optional: Enable debug logging
# DEBUG=true
